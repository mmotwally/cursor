import { runQuery, runStatement } from '../../database/connection.js';

// Recursively calculate BOM cost
export async function calculateBOMCostRecursive(bomId) {
  // ... implementation from boms.js ...
}

// Update BOM costs
export async function updateBOMCosts(bomId) {
  // ... implementation from boms.js ...
} 